export interface Client {
    id:number;
    name:string;
    email:string;
    isOften:boolean;
    orderIds:number[];
}
export interface newClient {
    name:string;
    email:string;
    isOften:boolean;
}

